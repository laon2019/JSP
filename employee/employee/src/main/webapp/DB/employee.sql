create table employee(
	num int(5) primary key not null Auto_increment,
	id varchar(30),
	pass varchar(30),
	name varchar(30),
	gender varchar(10),
	lev varchar(10),
	phone varchar(20),
	enter varchar(30)
);
